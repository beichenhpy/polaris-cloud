server:
  port: 23331
spring:
  application:
    name: consumer
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        # dataId:${prefix}-${spring.profiles.active}.${file-extension}
        server-addr: localhost:8848
