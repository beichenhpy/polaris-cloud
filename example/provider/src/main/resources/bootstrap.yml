server:
  port: 23338
spring:
  application:
    name: provider
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        # dataId:${prefix}-${spring.profiles.active}.${file-extension}
        server-addr: localhost:8848